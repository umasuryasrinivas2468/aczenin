# CLI scratch state: linked project ref, cached version probes, pooler URL.
# Regenerated by `supabase link`, so committing it only creates merge noise
# and risks leaking connection details into the repo.
.temp

# Local-stack secrets written by `supabase start`. Never belongs in git.
.env

# Local branch state for `supabase branches`. Machine-specific.
.branches
